version: "3.9"

services:
  parlant-bridge:
    build: .
    ports:
      - "8083:8083"
    environment:
      BRIDGE_PORT: "8083"
      BRIDGE_HOST: "0.0.0.0"
      BRIDGE_DB_PATH: "/data/bridge.db"
    volumes:
      - ./data:/data
